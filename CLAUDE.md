# CLAUDE.md — LassDas

このリポジトリで作業するときに最初に読むもの。

---

## 最上位命題（これに反する実装・設計・見積もりはすべて誤り）

> **最小限の設定だけして、チケットを切ったら、よしなに質問したりしながら勝手に納品先まで上げてくれるシステム。**

判断に迷ったら必ずこの一文へ戻る。細部の議論がこの一文から逸れていたら、細部ではなく前提が間違っている。

---

## 消費側の事情をフレームワークに埋め込まない

**目の前の 1 案件に合わせた機械を作るな。** 作る前に必ず問え:

> **これは誰のプロジェクトでも成立するか？**

成立しないなら、それは次のどちらかである。

1. **設定として外から受け取るもの** — フレームワークが中身を知る必要はない
2. **そもそも作る必要がないもの** — 読めば分かる、渡せば済む

### 実際にやった違反（すべて同一セッション・2026-08-05）

| やろうとしたこと | 実際は |
|---|---|
| 「どのリポジトリかを依頼から判定する段」を設計する | **チケットに書いてある。** 読めばいい。読めなければ聞く |
| 「対象の固定を外すのに半日」と見積もる | **契約に項目が 1 つ増えるだけ。** 触ってよい範囲は GitHub App のインストール先が既に決めている |
| 消費側の知識（`CLAUDE.md` 相当）を secret に本文ごと埋め込む | **在り処を受け取れば足りる。** 本文を持つとドリフトし、サイズ上限で破綻する |
| この規律を全プロジェクト共通のルールへ昇格させる | **発火するのはフレームワーク設計時だけ。** 置き場所を間違えると drift を持ち込む |

4 件とも「自分で問題を作って、自分で見積もった」形になっている。**新しい機構を思いついたら、まず それが要らない可能性 を潰せ。**

### 消費側の前提知識について（一般形）

**知識には 2 つの経路がある。片方だけでは成立しない。**

| 経路 | いつ効くか |
|---|---|
| **渡す（取り込み）** | 既に書かれた知見がある場合 |
| **溜める（蓄積）** | 渡されていない / 足りない分 |

渡すだけでは、更新が止まった瞬間に古くなる。溜めるだけでは、既にある知見を毎回聞き直すことになる。

#### 渡す

消費側は「自分のプロジェクトの前提知識が**どこにあるか**」を指す。

- **形式は問わない** — `CLAUDE.md` かもしれないし `CONTRIBUTING.md` かもしれないし Wiki の書き出しかもしれない。ファイル名を決め打った時点で、その形式を採用している人にしか使えなくなる
- **在り処は 2 種類あり、区別しない**
  - **リポジトリの中** — パスを指すだけ。checkout 済みなので追加の仕組みは要らない
  - **リポジトリの外** — 本文を渡してもらう。**そこにしか無いものは、写す以外に手がない**
- **無くてもよい** — 無いことは失敗ではない。質問が増えるだけ

> **注意**: repo 内にあるものを本文で渡すと、更新のたびに同期が要りドリフトする（config のダイジェスト固定と同じ失敗）。**repo 内なら在り処、repo 外なら本文。** ここを一緒くたにしない。

#### 溜める

**質問の答えは、書かれていなかった知識そのものである。** 捨てずに、消費側のリポジトリへ書き足す PR を出す。

出し方は既にあるものと同じで、新しい機構は要らない。成果物が「コードの変更」か「知識の追記」かの違いだけ。

これにより、git の外にあった知識が git の中に入り、バックアップ・共有・レビューが既存の仕組みに乗る。**同じ質問が繰り返し出ることは、書き残すべき知識がそこにあるという信号。**

#### 正直な限界

- 使い始めがいちばん質問が多い。渡せるものを渡しておけば減るが、ゼロにはならない
- 渡された知識をどれだけモデルに載せるかは**別の問題**。全部を毎回渡せば費用も判断精度も落ちる。先回りして解かず、実測してから決める

---

## 入力について

**フレームワークは「何を入力とするか」を所有しない。**

- 書式を要求しない。起票された依頼をそのまま受け取る
- 読めない部分は**原則として選択肢で**聞く（1 行で答えられる形）。自由記述で問い返すのは選択肢を立てられないことが明確な場合だけ
- **入口で弾く経路を持たない。** 拒否してよいのは処理不能な入力（空・巨大・不正 UTF-8・制御文字）だけで、書式は拒否理由にならない
- 選択肢を出せないまま「どうしますか」と聞くのは判断の丸投げ

**フレームワークが所有するのは、レビュー段数・収束判定・納品先へ渡す対象・失敗時に何を残すか。** 入力の枠を外すぶん、敵対レビューの重みが増す。これは省略可能な付加機能ではなく、人手を挟まず出すことの根拠そのもの。

---

## 名前について

**`LassDas` は本フレームワークの公開名（2026-08-17 確定）。** 公開前はコードネームとして非公開だった経緯があり、「顧客向けの表面に基盤の名前を出さない」規律は目的を変えずに継続する:

- 消費側リポジトリ・Lambda・IAM・ログ・バイナリの静的情報・トラッカーへ投稿するコメントに出さない（顧客表面は `ticket-automation` 系の中立名）
- 生成物への混入は `config/m1-consumer.json` の `forbidden_candidate_text` が機械拒否する（大小無視）

---

## 詳細

- 設計の正本: [README.md](README.md)
- Codex 向けの同内容: [AGENTS.md](AGENTS.md)
