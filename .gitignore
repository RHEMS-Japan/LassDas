.DS_Store
/controller
/worker

# setup wizard resumable state (per-operator, never committed)
lassdas-setup.state.json

# setup wizard local build output
/setup

# console: the local binary and the frontend toolchain stay out of the tree
/console
cmd/console/web/node_modules/
