# AGENTS.md — LassDas

## 最初に読むもの

**[CLAUDE.md](CLAUDE.md) を読むこと。** 最上位命題、設計規律、入力の扱い、名前の制約はすべてそこにある。

内容をここへ書き写さない。二重に持つと必ずずれる。

## Codex 固有

- コミット件名は `Codex:` で始める
- PR タイトルは `[Codex]` で始める
- 既にマージ済みのものを、この規約のためだけに書き換えない。次の commit / PR から適用する

## 特に外してはいけない一点

> **これは誰のプロジェクトでも成立するか？**

新しい機構を思いついたら、まず**それが要らない可能性**を潰す。消費側の事情に合わせた機械を作った時点で、フレームワークではなくなる。違反の実例は [CLAUDE.md](CLAUDE.md) に記録してある。
